import React from 'react'


const Home = () => {
    return (
        <React.Fragment>
            {/* {state.activities.map((r, i) => {
                return (<div key={i}>
                    {r.title}
                </div>
                );
            })} */}
        </React.Fragment>
    )
}

export default Home